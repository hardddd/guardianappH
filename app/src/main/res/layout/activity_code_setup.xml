<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.CodeSetupActivity">

    <data>
        <variable
            name="viewModel"
            type="com.hardikkh.guardianapp.ui.CodeSetupViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <EditText
            android:id="@+id/pin_input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter 6-digit PIN"
            android:inputType="numberPassword"
            android:text="@={viewModel.secondCodeEntry}" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/confirm_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Confirm"
            android:textColor="@color/white"
            android:background="@color/black_forever"
            android:onClick="@{() -> viewModel.handlePinEntry(pinInput.text.toString())}" />
    </LinearLayout>
</layout>

